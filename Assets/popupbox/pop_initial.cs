using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class pop_initial : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject box;
    void Start()
    {
        box.SetActive(false);
    }
}
